const en = require("../messages/en.json");
console.log("operatorDashboard.overview.bookingsDesc:", JSON.stringify(en.operatorDashboard?.overview?.bookingsDesc));
console.log("adminDashboard.overview.kpiCards.bookingsDesc:", JSON.stringify(en.adminDashboard?.overview?.kpiCards?.bookingsDesc));
console.log("adminDashboard.overview.kpiCards.pendingVerificationsDesc:", JSON.stringify(en.adminDashboard?.overview?.kpiCards?.pendingVerificationsDesc));
