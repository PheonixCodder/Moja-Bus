/** Horizontal padding used by HomeView's contentContainerStyle.
 *  Kept here so components that need CARD_WIDTH can import it
 *  without creating a require cycle with home-view.tsx.
 */
export const H_PADDING = 20;
