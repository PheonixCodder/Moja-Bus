# Complete Driver Domain Workflow Matrix

This matrix traces every end-to-end operational workflow in the Driver Operations Domain from trigger to final state, detailing participating applications, APIs, database mutations, notifications, and failure recovery behaviors.

| Workflow Identifier | Start Trigger | Actors Involved | Frontend Layer | API / Backend Layer | Database Mutations | Notifications / Outbox | Failure & Recovery Handling | Complete? |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :---: |
| **WF-01: Driver Self-Registration** | Driver opens app & enters phone | Driver | `apps/driver-app/app/(auth)/register/*` | Better Auth OTP $\rightarrow$ `drivers.registerDriver` | `User`, `DriverProfile` (PENDING), optional `DriverCompanyAffiliation` | None (pending review) | Draft state persisted in `AsyncStorage`. Retry on network reconnect. | Yes |
| **WF-02: Operator Roster Onboarding**| Operator enters driver details | Operator | `apps/web` `AddDriverModal.tsx` | `drivers.createDriver` | `User` (if new), `DriverProfile` (PENDING), `DriverCompanyAffiliation` | SMS handoff text generated for operator copy | Ambiguous binding error prompt; explicit confirmation required for existing accounts. | Yes |
| **WF-03: License & Medical Verification**| Operator/Admin inspects documents | Operator / Admin | `VerifyDriverDialog.tsx` / `admin-driver-verifications-view.tsx` | `drivers.verifyDriver` / `admin.verifyDriver` | `DriverProfile` (VERIFIED/REJECTED), `AdminStaffActivityLog` | `driver-verification-outcome` | Mandatory doc existence check; reason required for rejections. Atomic rollback. | Yes |
| **WF-04: License Nightly Expiration**| Daily cron timer at 02:00 UTC | System Cron | N/A | `/api/cron/expire-driver-licenses` | `DriverProfile.verificationStatus = EXPIRED` | `driver-license-status` (EXPIRING_SOON / EXPIRED) | Idempotent monthly keys prevent duplicate spam. | Yes |
| **WF-05: Marketplace Offer Dispatch**| Operator clicks "Send Offer" | Operator | `SendOfferDialog.tsx` | `drivers.sendEmploymentOffer` | `DriverEmploymentOffer` (PENDING), `DriverOfferEvent` (SENT) | `driver-offer-received` | Rate capped (25 sent per company, 20 received per driver). Validates salary $\ge 1,000$. | Yes |
| **WF-06: Counteroffer Negotiation** | Driver counters salary/dates | Driver $\rightarrow$ Operator | `offers-view.tsx` `CounterSheet.tsx` | `drivers.respondToOffer(COUNTER)` | `DriverEmploymentOffer` (COUNTERED), `DriverOfferEvent` (COUNTERED_BY_DRIVER) | `operator-offer-countered` | Max 6 rounds enforced. 7-day rolling expiry refreshed. | Yes |
| **WF-07: Exclusive Offer Acceptance** | Driver accepts exclusive contract | Driver | `offers-view.tsx` | `drivers.respondToOffer(ACCEPT)` $\rightarrow$ `resolveAcceptance` | Displaced `DriverCompanyAffiliation` terminated, new affiliation created, Offer ACCEPTED | `operator-offer-accepted`, `driver-affiliation-ended` | Auto-terminates conflicting exclusive contracts; sends alerts to displaced carriers. | Yes |
| **WF-08: Direct Trip Assignment** | Operator assigns driver on trip | Operator | `OperatorTripDetailsView.tsx` | `trips.assignDriver` | `TripDriverAssignment`, `Trip.driverId` / `reliefDriverId` | `driver-trip-assigned` / `driver-dispatch-urgent` | Double-booking scan w/ 45m buffer. Deterministic Postgres `FOR UPDATE` lock order. | Yes |
| **WF-09: Urgent Dispatch Response** | Driver opens app with trip $<2$h away | Driver | `UrgentDispatchGate.tsx` | `drivers.getMyUrgentDispatches` $\rightarrow$ `drivers.acknowledgeUrgentDispatch` | `TripDriverAssignment.urgentDispatchAckAt` | None (already dispatched) | Persistent server-side acknowledgment. Clock skew vulnerability identified (P0-3). | Partial |
| **WF-10: Duty Shift Clock-In / Out** | Driver toggles duty switch on profile | Driver | `profile-view.tsx` | `drivers.toggleShift` | `DriverShift` (startedAt/endedAt), `DriverProfile.status` | None | In-flight trip blocks clock-out. Multi-affiliation requires selecting operator. | Yes |
| **WF-11: Trip Start & Token Mint** | Driver clicks "Start Trip" | Driver | `trips-view.tsx` | `drivers.startTrip` | `Trip.status = DEPARTED`, `DriverProfile.status = ON_TRIP`, `DriverProfile.currentTripId` | None | Mints 24h stateless HMAC telemetry token. Verifies driver is VERIFIED and assigned. | Yes |
| **WF-12: Live GPS Telemetry Stream** | Background task captures coordinates | Driver Mobile | `telemetry.ts` (Expo TaskManager) | `POST /api/v1/telemetry/ping` | `DriverLocationPing`, `DriverProfile.last*` | Redis: `trip:{tripId}:telemetry`, `operator:{companyId}:fleet` | Haversine 220 km/h jump filter. Offline queue (500 cap). Database lock contention under load (P0-2). | Partial |
| **WF-13: In-Flight Delay Reporting** | Driver reports traffic/breakdown | Driver | `live-view.tsx` `DelayModal.tsx` | `drivers.reportTripDelay` | `Trip.status = DELAYED`, `DriverLocationPing` (DELAY) | `operator-driver-assignment-conflict` | Automatically re-evaluates downstream driver assignments for new overlaps. | Yes |
| **WF-14: QR Ticket Scan Boarding** | Conductor/Driver scans passenger QR | Crew | `scanner-view.tsx` | `DriverCheckInService.scanCheckIn` | `Booking.boardedAt`, `Booking.checkedInAt` | None | Offline queue stored in `AsyncStorage`. Batch sync flushes with original physical scan times. | Yes |
| **WF-15: Pre-Departure Gate Boarding** | Conductor attempts boarding at gate | Conductor | `scanner-view.tsx` | `DriverCheckInService` | `Booking` | None | Blocked in mobile UI prior to trip departure (P0-1). | Broken |
| **WF-16: Mid-Route Relief Handover** | Primary driver hands over wheel | Crew | `live-view.tsx` | None | None | None | Missing runtime API and mobile UI handover trigger (P0-4). | Missing |
| **WF-17: Trip Arrival Finalization** | Driver clicks "Complete Trip" | Driver / Operator | `live-view.tsx` / Web Dispatch Board | `drivers.completeTrip` / `trips.updateTripStatus` $\rightarrow$ `convergeDriversAfterRunEnd` | `Trip.status = ARRIVED`, `DriverProfile.currentTripId = null`, `totalTripsCompleted += 1` | None | Anti-strand engine automatically resets status to AVAILABLE or OFFLINE based on open shift. | Yes |
| **WF-18: Driver Stats Nightly Reconcile**| Daily cron timer at 03:00 UTC | System Cron | N/A | `/api/cron/reconcile-driver-stats` | `DriverProfile.averageRating`, `totalDistanceKm`, `safetyScore` | None | Computes daily capped penalties ($-20$/day), proportional relief distances, clean streaks. | Yes |
