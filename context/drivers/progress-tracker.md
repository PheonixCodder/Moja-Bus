# Moja Bus Driver System — Progress Tracker

Update this file after every completed feature. Any AI agent reading this immediately knows what is done, what is in progress, and what is next.

---

## Current Status
- **🏁 v2 REMEDIATION COMPLETE — ALL 39 PHASES (00–38) CLOSED (2026-08-25)**: every one of the **93 findings** is fixed in code. Phases 00–31 + 34+35 landed in tracked sessions; **32, 33's remaining legs (PS-10 taxonomy + PS-11 guest-claim), 36, 37, 38** landed via parallel sessions and were code-verified into the record on 2026-08-25 (timing-safe webhook compare at paystack-client.ts; ticket-sheet QR caption = bookingReference not bearer token; `phonesMatch` guest-claim predicate + COMPLETED status reads in booking-read-service; `cron-hygiene.test.ts`; TELEMETRY_TOKEN_SECRET env docs; junk files removed; zero `$queryRawUnsafe` + day-dedupe escrow alerts; web tickets view + traveler tracking screen fully i18n'd). **The remediation track is over — what remains is staging & release only**: Gate A/B/C probes per `full-system-e2e-audit/12-release-checklist.md`, CSP report-only → enforcing flip after a zero-violation console pass, EAS build proof for the rnmapbox pair, and physical-device legs (camera scan, Novu delivery, Paystack checkout, push tap-routing QA on driver/traveler/web).
- **⚠️ SUPERSEDED FOR RELEASE-GATING by v2 audit (2026-08-22)**: `context/drivers/full-system-e2e-audit/` — six-agent full-system re-audit; **93 unique findings: 1×P0 · 10×P1 · 31×P2 · 51×P3**. Headline: migration tree unreproducible from git (P0), scanner can't read issued QRs, check-in tenancy holes, verifyPayment ownership hole, PAYSTACK refunds never settle, driver-stranding ON_TRIP, 2 dead notification schemas. Gate list: `full-system-e2e-audit/12-release-checklist.md`.
- **v2 remediation progress**: **Phases 34+35 CODE COMPLETE (2026-08-25)** — notification small-fixes + web security headers/origins. F-NF-11/12/13/14/15 + F-IN-08/09/10/16 closed. Key rulings executed: retry resets attempts:0 (D34-1a); pauseReason rename + harness rows, D34-2b-i follow-up filed in-code ("discounts notify family → outbox" = its own session); NF-13 pre-closed by Phase 24, same-day re-suspend edge documented; conflict-alert email optional both sides + ISO busyUntil ride-along; F-NF-15 tap navigation via client-side identifier→route maps on all THREE surfaces (map wins over redirect fallback; cross-surface redirects never followed; suspended-operator no-nav pin on web; Novu SDK verified to carry identifier+data+redirect so no workflow edits needed); driver notifications got own i18n namespace. Phase 35: CSRF extracted to lib/mutation-origin.ts (malformed→FORBIDDEN; prod pins https scheme; no-Origin bypass documented for natives); images.remotePatterns first-party only, editor surfaces use `unoptimized` prop instead of widening allowlist; Caddy Permissions-Policy geolocation=(self)/camera=(self)/microphone=() SITE-block-only (un-broke browser QR scanner + geo-capture); CSP REPORT-ONLY baseline w/ compose-fed POSTHOG_PUBLIC_HOST/S3_PUBLIC_HOST defaults — enforcement flips after zero-violation staging QA, nonce/hash CSP deferred to own session; trustedOrigins prod-gated via lib/trusted-origins.ts (localhost defaults + exp:// dev-only; EXPO_DEV_ORIGIN = explicit recovery path). ⚠️ GATE LESSON: first turbo run showed green from CACHE while real errors existed beneath — always verify Cached:0 or --force. Final gates: turbo typecheck+test **19/19 UNCACHED** · web **543/543** · driver-app **20/20** · schemas 86 · biome clean on new files · migrations ZERO. Staging legs added: header dump, geo-capture QA, browser scanner QA, OTP login regression both apps, CSP console review.
- **v2 remediation progress**: **Phases 28+29+30+31 COMPLETE (2026-08-25)** — telemetry parity/observability + mobile-map compliance + driver data honesty. Closed: F-TM-07/08/09/13/14/15/16/17/18/19, F-DV-11/14, +4 unnumbered observations (stale-terminatedAt verified PRE-closed by Phase 26). **28/29**: shared prev-point store puts the Haversine jump gate on BOTH transports (DriverProfile.last* read-through; WS seeds at connect) w/ anti-evasion reference rule; >50 m accuracy no longer rejects — LOW_ACCURACY stamped by derivePingAnomaly (precedence over overspeed), persisted unscored, excluded from last-position/references; Redis delete-arm (write-only hash + dead GEOADD gone) w/ loud backend banner, bounded boot retries, /api/health surface, REDIS_URL/KV_URL passthrough; JSON observability on every reject/stamp + driver Telemetry Health card; reconcile segment-fair distance (city-chain RATIO × road km), clean-trip = ≥1 ping AND zero penalized anomalies, sorted FOR UPDATE flush locks, tagged $queryRaw ride-along. **30**: tracking button pushes booking.tripId (payload already carried it — audit's server task moot); Mapbox logo/attribution ON both apps + Leaflet control restored on route-map-preview AND search-map-view (**review residue: repo-wide grep caught the 4th surface the audit missed**, incl. deliberate `.leaflet-attribution-flag` CSS suppression); route-cache TTL 24 h via pure `mapbox-cache-core.ts` (v2 prefix orphans eternal entries), overview=simplified, fallback = null metrics + isApproximate → "Itinéraire approximatif" chip; @rnmapbox/maps exact-pin 10.3.5 (pair in library-docs.md; EAS proof = staging leg). **31**: getMyEarnings SQL aggregates over unbounded windows (FILTER clauses, open-shift live accrual, ISO Monday-start), rate → PlatformSettings.driverPayRateXofPerMinute (migration; env-var REJECTED — F-IN-11 drift class), "Guaranteed Payout"→"Estimation" + isPlaceholderRate; urgent-dispatch acks SERVER-SIDE (urgentDispatchAckAt column + mutation; AsyncStorage gate swapped; **ACK-RESET ruling recorded in-code**: in-window delays intentionally stay silent — trips surface owns delay comms); departureTimeIso replaces fr-FR string (device-locale render); odometer accepted-discarded fields deleted; broadcastTripAnnouncement echo stub deleted (zero callers); getMyTrips ALL excludes CANCELLED; **D8-a executed: updateDriver.status STRIPPED** (sole UI caller never sent it; generic write bypassed Phase-06 convergence). Review residue: earnings GLOBAL-across-carriers scope ruling + ack-reset semantics both recorded as in-code comments. Fixed-in-passing (pre-existing): web tsc breakage left by the interrupted 28/29 session (bad @prisma/client import + WS lastPing typing), dead penaltyRows query burning DB every night, driver-app biome.jsonc extending nonexistent root config + missing root:false (**driver lint had never once run**). Gates: turbo typecheck+test **19/19** · web **496/496** · driver-app **14/14** (+4 cache-envelope tests) · biome clean on new lines. Migrations ×2 → ✅ **deployed to Neon 2026-08-25** (user-run `migrate deploy`; exactly these two were pending — tree fully converged).
- **v2 remediation progress**: **Phases 28+29+30+31 COMPLETE (2026-08-25)** — telemetry parity/observability + mobile-map compliance + driver data honesty. Closed: F-TM-07/08/09/13/14/15/16/17/18/19, F-DV-11/14, +4 unnumbered observations (stale-terminatedAt verified PRE-closed by Phase 26). **28/29**: shared prev-point store puts the Haversine jump gate on BOTH transports (DriverProfile.last* read-through; WS seeds at connect) w/ anti-evasion reference rule; >50 m accuracy no longer rejects — LOW_ACCURACY stamped by derivePingAnomaly (precedence over overspeed), persisted unscored, excluded from last-position/references; Redis delete-arm (write-only hash + dead GEOADD gone) w/ loud backend banner, bounded boot retries, /api/health surface, REDIS_URL/KV_URL passthrough; JSON observability on every reject/stamp + driver Telemetry Health card; reconcile segment-fair distance (city-chain RATIO × road km), clean-trip = ≥1 ping AND zero penalized anomalies, sorted FOR UPDATE flush locks, tagged $queryRaw ride-along. **30**: tracking button pushes booking.tripId (payload already carried it — audit's server task moot); Mapbox logo/attribution ON both apps + Leaflet control restored on route-map-preview AND search-map-view (**review residue: repo-wide grep caught the 4th surface the audit missed**, incl. deliberate `.leaflet-attribution-flag` CSS suppression); route-cache TTL 24 h via pure `mapbox-cache-core.ts` (v2 prefix orphans eternal entries), overview=simplified, fallback = null metrics + isApproximate → "Itinéraire approximatif" chip; @rnmapbox/maps exact-pin 10.3.5 (pair in library-docs.md; EAS proof = staging leg). **31**: getMyEarnings SQL aggregates over unbounded windows (FILTER clauses, open-shift live accrual, ISO Monday-start), rate → PlatformSettings.driverPayRateXofPerMinute (migration; env-var REJECTED — F-IN-11 drift class), "Guaranteed Payout"→"Estimation" + isPlaceholderRate; urgent-dispatch acks SERVER-SIDE (urgentDispatchAckAt column + mutation; AsyncStorage gate swapped; **ACK-RESET ruling recorded in-code**: in-window delays intentionally stay silent — trips surface owns delay comms); departureTimeIso replaces fr-FR string (device-locale render); odometer accepted-discarded fields deleted; broadcastTripAnnouncement echo stub deleted (zero callers); getMyTrips ALL excludes CANCELLED; **D8-a executed: updateDriver.status STRIPPED** (sole UI caller never sent it; generic write bypassed Phase-06 convergence). Review residue: earnings GLOBAL-across-carriers scope ruling + ack-reset semantics both recorded as in-code comments. Fixed-in-passing (pre-existing): web tsc breakage left by the interrupted 28/29 session (bad @prisma/client import + WS lastPing typing), dead penaltyRows query burning DB every night, driver-app biome.jsonc extending nonexistent root config + missing root:false (**driver lint had never once run**). Gates: turbo typecheck+test **19/19** · web **496/496** · driver-app **14/14** (+4 cache-envelope tests) · biome clean on new lines. Migrations ×2 → ✅ **deployed to Neon 2026-08-25** (user-run `migrate deploy`; exactly these two were pending — tree fully converged).
- **v2 remediation progress**: **Phases 25+26+27 complete (2026-08-25)** — Wave-4 opener trio executed; F-OP-09…15 all closed, catalog P2/P3 driver-domain set now fully landed. Phase 25: `drivers:verify.read|manage` keys + requireAdminPermission on both hub procedures + sidebar gating (**ADMIN template seeded** — without it the empty-permissions fallback stripped the hub from every non-SUPER_ADMIN at deploy); activity log INSIDE the flip tx; driver outcome notice via NEW durable outbox type `DRIVER_VERIFICATION_OUTCOME` in-tx (first session's direct novu.trigger replaced — F-NF-class defect) + contract-harness row; getPublicDriverProfile CONDITIONAL redaction via shared view type (first session's unconditional version had killed contact data platform-wide, hardcoded isOnMyRoster:false regressing F-OP-06, and silently deleted trustBadges behind an `as any`) + sheet "Off Market" CTA. Phase 26: unassign window mirrored to SCHEDULED/DELAYED/BOARDING (ARRIVED/CANCELLED immutable); createDriver $transaction + separate email/phone match + AMBIGUOUS_BINDING client dialog (no auto-resolve) + terminatedAt clear; add-driver modal licence uploads via Phase 15 purposes (operator-namespace ruling documented) + APPROVE-without-docs refused on BOTH verify paths + admin button disabled until docs; **schema fix**: createDriver/updateDriver `.url()` constraint would have rejected every Phase-15 storage key — new `driverDocReferenceSchema` accepts keys-or-https. Phase 27: N+1 conflict scan → ONE batched query over shared pure core `findTripConflict` (+ deterministic orderBy both paths + urgent feed), SUSPENDED/verification/employment roster filters exposed, canAssign re-backed to trips:update (was mislabelling capable dispatchers), RIDE-ALONG: missing `licenseExpiryDate` select made Phase 14's licence gate inert on listAssignableDrivers — fixed. Gates: schemas tsc ✓ · web tsc ✓ · turbo test **9/9 · web 461/461** · biome clean on new lines.
- **v2 remediation progress (prior)**: **Phases 23+24 complete (2026-08-23)** — Phase 23: simulated radar DELETED from the operator fleet screen; new `fleet-live-map.tsx` (react-leaflet, CARTO dark tiles, ssr-false dynamic import per in-house pattern) plots ON_TRIP/ON_DUTY drivers from the existing 10 s getLivePositions poll with three-state freshness (fresh ≤5 min / dimmed ≤24 h / hidden >24 h), popups (name·plate·speed·ping), selected-vehicle HUD. Honest "10 s refresh" labeling replaces streaming pretense. Phase 24: public-profile sheet Send-Offer disabled when `isOnMyRoster` (procedure response now exposes it via companyId in affiliations select); sent-offers accumulate pagination (page batches + reset on tab change); marketplace featured/suspended keys day-bucketed (F-NF-13 remainder closed). Gates **19/19** · web 447 · biome clean.
  - **Phase 22 complete — NOTIFICATION SUBSYSTEM FULLY HARDENED (2026-08-23)** — F-NF-07: notifyExhaustedCampaignBudgets console.log orphan replaced with durable outbox enqueues (`CAMPAIGN_BUDGET_EXHAUSTED`, day-bucketed per operator, recipients via companyOperatorRecipients); F-NF-08 ruling EXECUTED = DELETE admin-bank-account-pending (Paystack self-verifies at save ⇒ no honest pending event exists; registry now zero unexplained entries; user to archive Novu-dashboard twin); F-NF-10 resolved by evidence + guard: prod crontab ALREADY every-minute (≈1 min latency beats */5 ask), new outbox-cadence-guard test locks the line into CI (vercel.json documented as non-prod reference, hourly forced by Vercel free tier). Gates **19/19** · web **447/447** · biome clean.
  - **Phases 20+21 complete (2026-08-23)** — Phase 20: recipient-scoped txIds inside the 5 multi-recipient offer helpers + conflict alert (truncated SHA tag via `txIdWithRecipient`/`recipientTag`, collision-freedom unit-tested), both lazy-expiry sweeps routed through `expireOfferIfDue` in-tx (F-DV-13 closed), `dashboardUrl()` util fixing payout-failed + verification-approved/rejected hardcoded hosts (approved CTA was pointing operators at the ADMIN surface — copy-paste bug corrected). Phase 21: expo.data overrides on all 11 traveler-audience push workflows matching the handler map verbatim; NEW push steps added to 7 driver workflows (offers ×4, assigned/unassigned, dispatch-urgent) with fr-first copy; driver handler gained the missing `dispatch-urgent` case → /(tabs)/trips. F-NF-06: registerPushToken switched replace→**append** (dual-app users keep both tokens; platform input documented informational). Gates **19/19** · web **445/445** · biome clean.
  - **Phases 18+19 complete (2026-08-23)** — F-PS-05 notice half verified ALREADY CLOSED by Phase 04's pull-forward; deleted the dead wallet router (zero callers anywhere, callback pointed at a nonexistent page). Traveler cancel dialog now renders the server's per-seat refund quote (`passenger.getRefundQuote`, WALLET-only) with non-cancellable note + toast error surfacing. Deep-link fixed to `/booking/[reference]`. Review integrity: CTA → getAppOrigin()/dashboard/bookings?tab=past (+ in-app redirect aligned) with payloadSchema extracted and contract-harness row added; traveler ReviewSheet explicit-only (overall starts 0/disabled, sub-ratings null + keys omitted); submitReview rejects non-completed trips via completedAt predicate (same source as getPendingReviews). Gates **19/19** · web **441/441** · biome clean.
  - **Phases 14+15+16+17 all complete (2026-08-23)** — Phase 14: trip-aware licence gates (vs estimatedArrival — D1 corrected from naive "expiry>now") on assign/listAssignable/urgent-feed/startTrip, VERIFIED-only operate policy in driverProcedure denylist, nightly `expire-driver-licenses` cron (VERIFIED→EXPIRED flip + month-bucketed 30-day warnings, both schedule sources), amber/red expiry badges. Phase 15: wizard uploads via 4 new private user-scoped storage purposes (failed upload blocks Continue; no more `file://` as document URL), `nationalIdNumber` persisted (migration rehearsed drift-0), employmentType honored into servicePreference + affiliation, `affiliated` flag + honest no-carrier state, presigned GET dossier rendering + legacy-URI placeholders; rider: documents.tsx `<div>` Android crash fixed. Phase 16 (D3 corrected: name/avatar overwrite freely, phone gated): updateMyStatus one-authority-per-transition matrix, INVITABLE_STAFF_ROLES excludes DRIVER server-side across invite/role schemas + shared UI constant, role input:false, phone E.164 + PHONE_REVERIFICATION_REQUIRED structured error parsed by wizard, OTP log env-gated. Phase 17 (D6 corrected: naive minutes-write would double-count vs operator formalization): reportTripDelay mirrors the operator formula exactly (cumulative+DELAYED pre-departure+time-shift+conflict alerts; DEPARTED keeps status), ledger deterministic (hiredAt fallback, double-open REJECT, close bound to company) with partial-unique-index migration incl. duplicate-repair. Gates **19/19** · web 440 · driver-app 10 · schemas 86 · biome clean · BOTH new migrations rehearsal-proven.
  - **NEON MIGRATED ✅ (2026-08-23)** — backup branch taken by user; six pending migrations + repairs + convergence applied live. First attempt hit the db-push-mixed reality (`BannerActionType` pre-existed) → recovered via `resolve --rolled-back` + hardened idempotent convergence (equality-checked enum rebuilds, guarded creates; fresh path re-proven drift-0 on scratch). Post-state verified exact; **probes 6/6 re-run against real Neon**; pb_ seed swept, residue ALL CLEAN. Vercel testing server unblocked — user smoke test next. Custom server pending introspection + run. NOTE: migrations 20260824000000/01 (Phases 15/17) landed AFTER the Neon run — ✅ RESOLVED 2026-08-25: user-run `prisma migrate deploy` applied the final ×2 phase-31 migrations (`20260825000000/01`); **Neon fully converged (31/31)**. Custom server still introspection-pending.
  - **Phases 13+12+11 all complete (2026-08-23)** — Phase 11 initially deferred, then reversed after decomposition review (operator-subscriber design was never in its scope): token gains signed `c` claim at both mints (`mintTelemetryDispatchTokenWithCompany`, backward-compatible verify), enforced-mode `subscribe` restricted to claims-derived rooms only (`isRoomAllowedForClaims`), fleet publish flows from claims, HTTP-gap documented in-code; new 8-case token/ACL suite. Phase 13: passport Edit dialog + Remove-from-roster with server-side mid-run CONFLICT guard, atomic offboard+notice via new `driver-roster-removed` workflow + `enqueueDriverRosterRemoved`, roster accumulate pagination, F-IN-02 ride-along. Phase 12: live HUD real GPS (simulator deleted), honest Trip ETA from Directions durationSeconds, "Fixed 5 s" label, zombie-stop preserved. Gates **19/19** · driver-app 10/10 · web **440/440** · biome clean. Revival checklist shrinks to: host image + Caddy passthrough + operator-subscriber credentials + consumer client.
  - **Phases 09+10 executed together (2026-08-23)** — D1 ruling after challenge: **Option B ratified** (HTTP-only v1; B is the zero-cost-reversal prefix of a future A). WS localhost default removed (skipped unless `EXPO_PUBLIC_WS_URL` set), reconnect budgeted 5×exp-backoff/segment, dormancy+revival checklists on gateway/server/env. Phase 10: chunked ≤100 offline drain w/ preserved remainder + logged drops; **D5 corrected from audit text** — deceleration-severity harsh braking (≥25 km/h AND ≥2.8 m/s² AND Δt≤8 s) instead of naive window-widen that would flag everyday braking; 401 self-heal via injected re-mint handler (`getTelemetryToken`) + needsReauth health flag. Coupling fix: flush no longer WS-open-only (start/ping-success/60s sweep) — without it Option B breaks queue draining. New pure module `telemetry-core.ts`; driver-app test runner wired into turbo (first mobile suite): gates **19/19**, driver-app 10/10, web 432/432. Staging legs: radio-log (no futile WS), outage-drain, device brake QA.
  - **Phase 08 (subscriber identity completion, F-NF-03) code complete 2026-08-23** — all nine broken sites re-keyed to user.id per ratified D1–D7: invitation staff-acceptance (+invitedBy.id now selected; was falling back to invitee's email), profile-updated, review-submitted (both now log loudly, no swallow), user-role-updated, account-suspended/restored loops, withdrawal-resolved, FORCE_FAIL payout fan-out, and the escrow treasury alert (email/id split closed vs operator.ts). Adjacent Date.now() txIds replaced with stable/day-bucketed keys on every touched line (**escrow daily-resend = F-IN-13 closed early**); contract harness extended to 19 cases covering all 9 audience workflows (schemas exported as consts). Gates 18/18 · web 432/432 · biome clean. Follow-up filed: admin fan-out audience policy (role=ADMIN vs active AdminStaff). Staging leg pending (Novu staging: suspend/payout events → badge+push).
  - **Phase 00 rehearsal EXECUTED and PASSED (2026-08-23)** — clean-volume replay initially FAILED (`relation "Operator" does not exist`); root cause: phase17_cleanup hand-wrote Prisma model names ignoring @@map ("operator"/"user"/"driver_profile") — fixed in place (never-applied exception, ruling in file header). Drift enumeration then exposed MORE holes → added `20260823235959_phase00_schema_convergence`: promo_banner+BannerActionType (model had NO migration!), six enum rebuilds to exact datamodel sets, default alignments, license-'A'→'B' defensive map. **RESULT: full replay completes AND drift diff EMPTY (exit 0)** on real PG16. db-drift.yml also fixed for current CLI (old flags would have errored). Neon runbook now trivial: no resolves needed, just `migrate deploy` after backup branch. Scratch `moja_probe_tmp` = converged empty DB ready as Phase 02–06 probe target.
  - **Phase 07 (passenger notice schemas, F-NF-01/F-NF-02) code complete 2026-08-23** — rulings D1–D7 executed: cancelled payload carries bookingReference + unconditional refundAmountXOF + refundStatus/refundChannel enums with fully truthful template copy (wallet credit vs manual CASH settlement vs failed-refund lines); BOTH delay paths moved onto shared outbox helper `enqueuePassengerTripDelayed` (type TRIP_DELAYED, hourly-bucketed transactionIds, no more direct novu.trigger/.catch swallow); cancelled fan-out enqueued INSIDE the cancel tx (F-PS-14 closed for this surface); payloadSchemas exported as named consts + 9-case contract harness with audit-defect regression tripwires (`payload-contracts.test.ts`); SENT semantics documented at bridge. Gates 18/18 · web 422/422. Staging probes pending (cancel → 3 channels; delay → notified; second delay → next-hour bucket).
  - **Phase 03 (check-in authorization binding, F-IN-01 ≡ F-DV-03) code complete 2026-08-23** — shared `DriverCheckInService.assertBoardable` guard pipeline (tenancy via active TripDriverAssignment → FORBIDDEN · declared-tripId mismatch → BAD_REQUEST · CONFIRMED-only → PRECONDITION_FAILED · boarding window) wired into all three procedures with session-derived `ctx.driver.id`; manifest no longer returns durable ticketTokens (`drivers.ts:1324`); batch reports per-item outcomes instead of swallow-all. Ratified deviations: `tripId` stays optional in schema (server derives tenancy — stronger than client-supplied requirement); CONDUCTOR role boards (manifest-consistent); no post-arrival grace. 30-case suite green; gates 18/18. Staging probes pending per checklist F-IN-01.
  - **Phase 01 (CI quality gate, F-IN-05/F-IN-06) code complete 2026-08-23** — deploy.yml gate now typecheck (traveler-app included; D7-A probe clean exit 0) → `turbo test` → lint; all 4 orphaned suites resolved (outbox+authorize wired green · staff-hierarchy deleted per D5-A · roles-and-permissions rewritten to shipped RBAC model + wired into schemas; 56/56 suites wired, 0 orphans); local red/green proven (deliberate fail → exit 1 / restored → exit 0, 8/8 turbo tasks, web 413/413 · schemas 78/78). ⚠️ Deviation pending ratification: lint step landed report-only (`continue-on-error`) — pre-existing biome debt across all 10 packages needs its own sweep before it can block. CI end-to-end red/green + branch-protection flip (`quality-gate` + `drift-check`) = user actions.
  - **Phase 05 (refund channel truthfulness, F-PS-02) code complete 2026-08-23** — Option B removal arm ratified per product money model (money never flows back out through Paystack): `isCreatableRefundChannel` guard rejects PAYSTACK in `CancellationService`, `canPassengerSelfCancelWithChannel` → WALLET-only, web passenger dialog dead CASH toggle removed, ZERO_CASH settlements cancel without minting phantom refund obligations, refund notices carry true channel (was hardcoded PAYSTACK→"CASH"); OWED queue CASH-only filter now correct by design; DB verified zero PAYSTACK refund rows (no data fix needed); tests 15/15 phase suite + 378/378 web. Staging probe pending per checklist F-PS-02.
  - **Phase 06 (driver run-state lifecycle, F-DV-04) code complete** — `lib/driver-run-state.ts` convergence (cancel + operator-ARRIVED + suspend/REJECT), shift-aware post-run status, type-aware suspended read-only surface, driver-app zombie-telemetry stop + SUSPENDED status screen; 7 unit tests green (`web` suite 339/339). Staging probe pending per checklist F-DV-04.
  - **Phase 04 (payment verify ownership, F-PS-01) code complete 2026-08-23** — service-level `verifyAndConfirmForUser`/`verifyAndConfirmSystem` split (ownership enforced before any Paystack call/state change), `rescueOrphanedPayment` always credits `holdGroup.userId` (expired-hold fund-diversion vector closed beyond phase text), F-PS-13 binding half pulled forward (`verifyTopUpForUser` + `userId` stamped at both top-up initiation sites); 11-test suite `reference-ownership.test.ts`, web suite green. Staging probes pending per checklist F-PS-01.
  - **Phase 02 (scanner ticket parsing, F-PS-03 ≡ F-DV-02) code complete 2026-08-23** — canonical `parseTicketToken` moved to `packages/schemas/src/ticket-token.ts` (host-agnostic URL / `?token=` / relative-path / JSON-wrapped / bare forms; regex-only so Hermes + DOM-lib-free compiles are safe; malformed `%zz` can no longer throw), wired via `z.preprocess` on `driverCheckInPassengerSchema` **and** each `batchSyncCheckIns` item; `pt.` presentation tokens resolve server-side through an injected `resolvePresentationToken` on `DriverCheckInService` (unresolvable pt. → per-item NOT_FOUND in batch); old `features/operator/lib/parse-ticket-token.ts` deleted, `operator-booking-service` re-pointed to `@moja/schemas`, client JSON unwrap removed from `scanner.tsx`. Tests: 20-case contract matrix (`schemas/__tests__/ticket-token.test.ts`) + 4 pt.-resolver service tests; gates green — schemas 54/54 · web 382/382 · tsc ×3 workspaces · biome clean on touched files. Composed cleanly with the parallel Phase-03 check-in-service refactor. Staging probe pending: scan real traveler QR → Boarding Cleared.
- **Known tracker drift**: Phase 18.4/18.5/18.6 and 19.2–19.4 checkboxes disagree between this file, remediation-plan.md and memory.md — code-truth per v2 audit: 18.4 mitigated-open, 18.5 partial (flush done/fanout open-by-design), 18.6 DONE, 19.2–19.4 substantively landed. Reconcile as part of Gate 0.
- **Current Track**: **RELEASE REMEDIATION — Phases 16–19** (all 15 build phases complete; audit found 40 findings now organized into 4 remediation phases)
- **Active Plan**: `context/drivers/remediation-plan.md` (task-level acceptance criteria) · Master phases: `docs/plans/.../13-phased-execution-master-plan.md` §16–19 · Evidence: `context/drivers/e2e-release-audit/`
- **Next Up**: **Phase 16 — Critical Blockers (Gate A)** — 8 tasks: telemetry identity fix, Complete Run wiring, Novu subscriber unification, exclusive-consent retry, earnings crash, two cron schedules, telemetry auth. Gate A must clear before ANY public traffic.
- **Post-launch roadmap** (features, not remediation): live-tracking consumer client + WS hosting scale-out · driver pay-rate model (replaces ×50/min placeholder) · route-experience matching engine · offline scan-queue UI.

---

## Remediation Progress

| Phase | Scope | Status | Done | Total |
|---|---|---|---|---|
| 16 | Critical Blockers (5×P0 + crons + telemetry auth) | ✅ Code complete | 8 | 8 |
| 17 | Security & Credential Integrity | ✅ Code complete | 5 | 5 |
| 18 | Reliability Hardening | ✅ Code complete | 6 | 6 |
| 19 | UX Polish Sweep | ✅ Code complete | 5 | 5 batches |

---

## Phase Checklist & Progress

### Phase 1 — Schema, Core Types & IAM Foundation
- [x] **01.01** Extend `StaffRole` with `DRIVER` in `packages/db/prisma/schema.prisma`
- [x] **01.02** Create `DriverProfile`, `DriverCompanyAffiliation`, `TripDriverAssignment`, `DriverLocationPing`, and `DriverShift` models in `schema.prisma`
- [x] **01.03** Update `Review` model with `driverRating`, `busRating`, `punctualityRating`, `driverId`, `tripId`, `busId`
- [x] **01.04** Run Prisma migration / client generation (`prisma generate`) and validated schema
- [x] **01.05** Define driver Zod schemas and validation in `packages/schemas/src/drivers.ts`
- [x] **01.06** Add `drivers:*` and `telemetry:*` IAM permissions and role defaults in `packages/schemas/src/permissions.ts`

### Phase 2 — Operator Web ERP: Drivers Module & Dispatch Board
- [x] **02.01** Create `apps/web/trpc/routers/drivers.ts` with procedures (`listDrivers`, `getDriver`, `createDriver`, `updateDriver`, `verifyDriver`, `getDriverAnalytics`, `getLivePositions`)
- [x] **02.02** Add "Drivers" item with steering wheel icon in `apps/web/features/operator/components/operator-sidebar.tsx`
- [x] **02.03** Build `apps/web/app/[locale]/dashboard/operator/(dashboard)/drivers/page.tsx` and `OperatorDriversView` component
- [x] **02.04** Build Driver Registration & License Upload Modal (`add-driver-modal.tsx`)
- [x] **02.05** Build `apps/web/app/[locale]/dashboard/operator/(dashboard)/drivers/[id]/page.tsx` with Driver Career Passport, License Document Inspector, and Reviews Breakdown
- [x] **02.06** Update `apps/web/app/[locale]/dashboard/operator/(dashboard)/staff/page.tsx` to support `DRIVER` role filtering and permissions
- [x] **02.07** Update Trip Dispatch Board (`apps/web/features/operator/views/operator-trips-view.tsx` and `trips.ts` router) with Driver selector, relief driver assignment, and license verification warnings
- [x] **02.08** Build Operator Live Fleet Telemetry Map View (`/dashboard/operator/drivers/map`) with real-time bus markers and speedometer HUD

### Phase 3 — Real-Time Telemetry & WebSocket Ingestion Gateway
- [x] **03.01** Build standalone / modular WebSocket Telemetry Gateway in `apps/web/server/telemetry-ws.ts`
- [x] **03.02** Implement session / token query handshake on WebSocket `upgrade`
- [x] **03.03** Implement Safarpay-inspired validation pipeline (accuracy $<50\text{m}$, speed $<200\text{ km/h}$, Haversine jump detector)
- [x] **03.04** Implement Redis Geo storage (`GEOADD`, `HSET driver:live`, and in-memory mock fallback)
- [x] **03.05** Implement Redis Pub/Sub broadcast channels (`trip:{tripId}:telemetry`, `operator:{companyId}:fleet`)
- [x] **03.06** Implement background batch flush worker from memory buffer to PostgreSQL `DriverLocationPing` table
- [x] **03.07** Implement HTTP POST fallback endpoint (`/api/v1/telemetry/ping`) for intermittent network handshakes

### Phase 4 — Driver Mobile App (`apps/driver-app`): Foundation & Auth
- [x] **04.01** Initialize `apps/driver-app` with Expo 57, Expo Router, NativeWind, Biome, and monorepo workspace packages (`@moja/schemas`, `@moja/shared`, `@moja/ui`)
- [x] **04.02** Configure `app.json`, `metro.config.js`, `tailwind.config.js`, and navigation layouts
- [x] **04.03** Implement Driver Auth Flow (`/(auth)/login`) with Phone/OTP and Password via Better Auth
- [x] **04.04** Implement Operator Company Switcher & On-Duty / Off-Duty Shift Toggle
- [x] **04.05** Build Driver Tabs Navigation: `Trips`, `Live Trip`, `QR Scanner`, `Profile & Career`
- [x] **04.06** Build Driver QR Ticket Scanner (`/(tabs)/scanner`) using `expo-camera` with instant audio/haptic feedback and offline ticket token cache

### Phase 5 — Driver Mobile App: Trip Execution & Background Telemetry
- [x] **05.01** Build Assigned Trips List (`/(tabs)/trips`) with Today, Upcoming, and Completed filters
- [x] **05.02** Build Trip Detail & Passenger Manifest screen (`/trip/[id]/manifest`) with search, seat labels, and boarding status badges
- [x] **05.03** Implement Dual-Mode UI Engine (Intercity formal terminal schedule vs Urban loop runner)
- [x] **05.04** Implement Background Location Engine using `expo-location` and `expo-task-manager` with battery-optimized ping intervals (5s moving, 30s idling)
- [x] **05.05** Implement Offline Sync Queue with local SQLite / AsyncStorage for ping buffering during low-signal zones
- [x] **05.06** Build In-Trip Incident & Delay Reporter modal with automated Novu passenger notifications
- [x] **05.07** Build Driver Career Passport (`/(tabs)/profile`) with lifetime statistics, badge achievements, and review summary

### Phase 6 — Traveler App Integration & 3-Way Review System
- [x] **06.01** Integrate Live Bus Tracking Screen in `apps/traveler-app/app/tracking/[tripId].tsx` with live moving vehicle icon, route polyline, and real-time ETA
- [x] **06.02** Add "Track Live Bus" button on active bookings in `apps/traveler-app/features/booking/screens/booking-detail.tsx`
- [x] **06.03** Build Multi-Criteria Review Modal (`Driver`, `Vehicle`, `Punctuality` 1–5 stars + written feedback)
- [x] **06.04** Implement automatic post-trip review prompt on Traveler App launch + Novu push notification upon trip completion
- [x] **06.05** Wire review submission to `apps/traveler-app/app/reviews.tsx` and `apps/traveler-app/app/(tabs)/bookings.tsx`
- [x] **06.06** Update Operator Reviews View (`apps/web/features/operator/views/operator-reviews-view.tsx`) to display driver breakdown, vehicle score, and filter by driver

### Marketplace Evolution — Phases 7–15 (details in `docs/plans/driver-app-and-telemetry-deep-audit/13-phased-execution-master-plan.md`)
- [x] **Phase 7** — Driver Phone OTP auth (Better Auth) + Admin Driver Verification Hub (`/dashboard/admin/drivers/verifications`, `admin.listDriversForVerification` / `admin.verifyDriver`)
- [x] **Phase 8** — fr/en i18n (i18next + expo-localization, French-first), Montserrat typography, CI-locked phone input
- [x] **Phase 9** — `DriverServicePreference` model + migration w/ backfill; `setServicePreference` / `getMyServicePreference` / `getPublicDriverProfile` / `listMarketplaceDrivers` tRPC; driver post-verification preferences screen + boot gate + Available-for-Hire toggle; admin marketplace health widget; `CONTRACTOR_URBAN` naming fix
- [x] **Phase 10** — `/dashboard/operator/drivers/marketplace` page (server-prefetched); card grid with 5 nuqs-persisted filters incl. rating/safety sliders; `MarketplaceDriverCard`; `DriverPublicProfileSheet`; "Talent" sidebar section; Send Offer = Phase 11 placeholder toast
- [x] **Phase 11** — Structured Offer Board: subscriber identity → user.id platform-wide; `DriverEmploymentOffer` + `DriverOfferEvent` audit models + partial unique index (one active offer per pair); 7 tRPC procs (send/respond×2/list×2/withdraw/markSeen) w/ rolling 7-day expiry + anti-spam caps; durable Outbox delivery + 13 registered Novu workflows (fr-first); driver app full Novu stack (push token, bell, /notifications inbox, offers tab w/ live badge, counter sheet, exclusive-conflict consent); operator Send Offer dialog (card+sheet wired) + Sent Offers dashboard (/drivers/offers: Seen chips, counter actions, withdraw); expire-offers cron
- [x] **Phase 12** — Dispatch Board Assignment: hardened `trips.assignDriver`/`unassignDriver` (status guards, cross-company interval conflict engine w/ turnaround buffer via `lib/driver-assignment.ts`, license gate vs `BusType.requiredLicenseCategory`, consent-gated slot replacement); `drivers.listAssignableDrivers`; trip-card Driver/Relief/Conductor rows w/ greyed-ineligible combobox; outbox ×3 dispatch workflows; driver UrgentDispatchGate (AsyncStorage acks, 60s poll) + trips 30s polling
- [x] **Phase 13** — Ratings & Trust: anomaly pipeline repaired (schema flags, server-recomputed overspeed, flush maps `isAnomaly/anomalyReason`, phantom battery columns removed); `lib/driver-scoring.ts` (−5/−10 penalties, −20/day cap, +1 per 10 clean ARRIVED trips); intraday capped deltas in flush tx + `reconcile-driver-stats` nightly full recompute (route-distance aggregation, driverRating-only semantics, first-run backfill); `submitReview` averaging fix; computed-on-read badges (Top Rated/Safe Driver/Veteran) on card+sheet+detail; `getDriverAnalytics` + recharts Insights tab
- [x] **Phase 14** — Admin Marketplace Controls: `marketplace:read|manage` permission keys; `setDriverMarketplaceStatus` (FEATURE w/ cap 20 / UNFEATURE / SUSPEND w/ mandatory reason + featured-slot release / RESTORE) + activity logging + outbox notifications (`driver-marketplace-featured/-suspended`); `listMarketplaceAdminDrivers` (incl. suspended/off-market); `getMarketplaceHealth` (funnel + time-to-hire + counter-rate); `listAllOffers` audit browser w/ negotiation timelines; `/dashboard/admin/drivers/marketplace` page (health strip + Drivers/Offers tabs) + gated sidebar entry
- [x] **Phase 15** — E2E Release QA audit: four parallel deep audits (operator / passenger+notifications / driver app / infra+security) → `context/drivers/e2e-release-audit/` (10 files, 40 findings w/ file:line evidence). Confirmed-solid: booking→ticket chain, ledger race-safety, offer-board integrity, RBAC, outbox guarantees. Found: 5×P0 + 7×P1 + 15×P2 + 13×P3.

---

## Release Remediation Track — Phases 16–19 (from Phase 15 audit)

> Task-level acceptance criteria live in `context/drivers/remediation-plan.md`; evidence in `e2e-release-audit/08-findings-catalog-p0-p3.md`.

### Phase 16 — Critical Blockers (Gate A)
- [x] **16.1** Real telemetry identity — remove `"drv_active"` fallback, thread driverProfileId through Start Run *(P0-1)*
- [x] **16.2** Complete Run → `drivers.completeTrip` mutation wiring *(P0-2)*
- [x] **16.3** Novu subscriber unification to `user.id` (`public.ts` getNotificationToken/registerPushToken + web Inbox keying) *(P0-3)*
- [x] **16.4** Exclusive-consent retry: confirm dialog → re-ACCEPT with `confirmExclusiveSwitch:true` *(P0-4)*
- [x] **16.5** Earnings `<div>` → `<View>` crash fix *(P0-5)*
- [x] **16.6** Schedule `expire-offers` cron in vercel.json *(P1-1)*
- [x] **16.7** Schedule `reconcile-driver-stats` cron in vercel.json *(P1-2)*
- [x] **16.8** Telemetry ingest auth: dispatch token minted at startTrip, enforced on WS + HTTP *(P1-4)*

**Gate 16**: full smoke script green (register→verify→offer→accept→assign→start→ping→complete→review) with correct ping identity; spoofed ping rejected. — *staging run pending; code complete + typecheck green*

### Phase 17 — Security & Credential Integrity (Gate B)
- [x] **17.1** Scope operator `verifyDriver` to company affiliation *(P1-3 IDOR)* — active-roster check mirrors getDriver/updateDriver; cross-company id → FORBIDDEN
- [x] **17.2** Operator-added driver credential handoff + existing-user binding confirmation *(P1-7)* — D3=guided handoff: `EXISTING_USER_BINDING_REQUIRED::name|phone|email|hasProfile` structured error + confirm dialog; success step shows phone-OTP login instructions with copy/share
- [x] **17.3** DRIVER staff over-provisioning fix + exclude drivers from `companyRecipients` *(P2-1)* — D2=no Operator rows for drivers (createDriver no longer creates them; migration `20260822000001` soft-deletes legacy rows); canonical `companyOperatorRecipients` helper excludes DRIVER and replaced the drivers.ts + expire-offers cron copies; D2b=new `UserRole.DRIVER` enum value for placeholder accounts (migration `20260822000000`)
- [x] **17.4** Workflow hygiene: ghost admin-staff acceptance ID, orphan bank workflows, ticket-share decision *(P2-2/P2-3)* — ghost → registered `staff-acceptance-alert`; `operator-bank-verified/-rejected` wired via durable outbox from verifyOperator/rejectOperator (D5); ticket-share email link fixed to APP_URL + locale prefix, Share UI shipped (web tickets sheet + traveler booking detail). ⚠️ `admin-bank-account-pending` deliberately left untriggered: both bank-save paths are Paystack-self-verifying (`isVerified:true` at save), so no honest "pending" event exists — ruling pending (wire at a future pending path or delete the workflow)
- [x] **17.5** Self-cancel refund notification via orphaned helper *(P1-6)* — D7=enqueued inside CancellationService transaction behind `notifyRefunded` flag; trip-cancel path suppresses (already notifies); guest bookings skipped (no email field)

**Gate 17**: code complete + typecheck green. Staging probes pending: cross-company verifyDriver → FORBIDDEN · add-driver binding-confirm flow end-to-end · refund notice delivered for WALLET + CASH self-cancels · driver placeholder lands role DRIVER with zero ERP access.

### Phase 18 — Reliability Hardening (Gate C)
- [x] **18.1** Outbox stale-PROCESSING reclaim (>15 min) *(P2-6)*
- [x] **18.2** process-outbox hourly cadence *(P2-7)*
- [x] **18.3** Assignment race safety: locks/isolation + one-active-exclusive constraint evaluation *(P2-8)*
- [ ] **18.4** WS hosting decision executed; simulated tracking off UX until real consumer *(P1-5)*
- [ ] **18.5** Redis fanout relay OR single-instance documentation + serverless-safe flush *(P2-10/11)*
- [x] **18.6** Baseline tRPC mutation rate limiting *(P2-15)*

**Gate 18**: concurrent duplicate assign loses cleanly; outbox survives kill -9; latency within bound. — *code complete (typecheck + 340 tests green); staging load pass pending*

### Phase 19 — UX Correctness & Polish Sweep (Gate D)
- [x] **19.1 Driver app**: delay modal submits (P3-12) · dual-mode filter (P3-13) · mobile .env.examples + Mapbox guard (P2-14)
- [ ] **19.2 Operator ERP**: passport affiliation scoping (P2-9) · HYBRID labels (P3-3) · KPI aggregates (P3-4) · own-roster CTA disable (P3-1)
- [ ] **19.3 Offers engine**: lazy-expiry audit parity (P3-2) · fare-derived durations + delay revalidation (P3-5) · bus-assigned via outbox (P3-6)
- [ ] **19.4 Passenger surfaces**: refund display (P2-12) · web driverRating (P3-7) · review prompt (P2-5) · low-balance alert (P2-4)
- [x] **19.5 Housekeeping**: unified cron-auth (P3-10) · artifact cleanup (P3-11) · ticket TTL decision (P3-8) · rate limit ✓ landed in 18.6

**Gate 19**: zero open P2/P3 without owner+date; full `pnpm build` green on release branch. — *code complete (typecheck ×3 + 340 tests green); Gate D review pending*

---

## Decisions Made During Planning
1. **Lifetime Portable Driver Identity**: The root `DriverProfile` is attached to the global `User` account so ratings, badges, and verified licenses follow the driver throughout their career across multiple operators.
2. **Dual-Mode Operational Engine**: Intercity vs Urban rules are decoupled in the schema via `DriverCompanyAffiliation.employmentType` and `Trip.serviceType`. Intercity operates under exclusive single-operator assignment with seat-level manifests, while Urban operates under a shared contractor driver pool with high-frequency loops.
3. **Enterprise Telemetry Ingestion**: Replaces basic in-memory WebSockets with a Redis Geo + PubSub architecture (inspired by the Safarpay reference) to support horizontal clustering, GPS jump anomaly filtering ($<50\text{m}$ accuracy gate, $<200\text{ km/h}$ speed gate), and dual-path HTTP/WebSocket fallbacks.
4. **3-Way Multi-Dimensional Passenger Review**: Reviews directly link to `driverId`, `busId`, and `tripId`, capturing 1–5 star ratings across Driver Behavior, Vehicle Comfort, and Punctuality.
