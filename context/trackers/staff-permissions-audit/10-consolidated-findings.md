# 10 — Consolidated Findings & Gaps (Live Tracker)

All findings from files 04–08, deduplicated, ranked by severity. Each finding cites the primary file(s).
**Status**: `Planned` | `In PR` | `Done` | `Blocked`

---

## CRITICAL

| # | Status | Finding | Plan | Log |
|---|---|---|---|---|
| C1 | Done | **`operator.saveOnboardingStep` writes company + bank account + Paystack recipient with no permission key** — only company-membership gate. Any staff member (even SUPPORT) can rewrite company legal details (`name`, `registrationNumber`, `taxId`, `slug`) and **re-register a Paystack transfer recipient**, redirecting payouts. | Added `requirePermission(ctx, "company:update")` to `saveOnboardingStep` + `reopenOnboardingStep` in `operator.ts`. OWNER (implicit-all) and ADMIN pass; others blocked. | Decision 1: gate = `company:update`. Files changed: `apps/web/trpc/routers/operator.ts`. |
| C2 | Done | **`operator.completeOnboarding` / `operator.resubmitVerification` flip company verification state with no permission key** — any member can set company to `PENDING_VERIFICATION` (bypassing admin review) or resubmit a REJECTED company. | Added `requirePermission(ctx, "company:update")` to `completeOnboarding` + `resubmitVerification` in `operator.ts`. | Decision 1: gate = `company:update`. Files changed: `apps/web/trpc/routers/operator.ts`. |

---

## HIGH

| # | Status | Finding | Plan | Log |
|---|---|---|---|---|
| H1 | Done | **`financials:view` is a ghost key** — settings sidebar Financials tab gated on a non-existent permission; effectively OWNER-only; FINANCE staff (server-authorized for `listBankAccounts`) can't see the tab. | 1. Added `"financials:view"` to `PERMISSION_META` in `permissions.ts` (Financials group). 2. Added to FINANCE template. 3. Updated `listBankAccounts` server gate to accept `financials:view` OR `company:view` via `requireAnyPermission`. 4. `settings-sidebar.tsx` already uses `perms.includes("financials:view")` — now works since the key exists. | Decision 2: add proper `financials:view` key. Files changed: `packages/schemas/src/permissions.ts`, `apps/web/trpc/routers/operator/settings.ts`. |
| H2 | Done | **`payments.cancelBooking` has no `bookings:update` guard** — any operator staffer can call it; relies on `CancellationService` to scope to the operator's company (verify it does; pattern divergence). | In `payments.cancelBooking`, after resolving `userCompanyId` for OPERATOR users, load operator profile and check `hasPermission(role, permissions, "bookings:update")` → throw FORBIDDEN if missing. Passenger (TRAVELER) cancellation untouched. | Files changed: `apps/web/trpc/routers/payments.ts`. |
| H3 | Done | **Overview page crashes for read-only roles** — sidebar always shows Overview, but `getDashboardMetrics` requires `trips:read \|\| bookings:read \|\| company:view`. `reviews:read`-only or `fleet:read`-only staff hit the error boundary on home. | 1. Gated Overview sidebar nav item on `permissions: ["trips:read","bookings:read","company:view"]`. 2. In `(dashboard)/page.tsx`, fetch `staff.getMyPermissions` server-side; if user lacks all three → render `<AccessDeniedCard />` instead of prefetching metrics. | Decision 4: AccessDeniedCard on Overview. Files changed: `features/operator/components/operator-sidebar.tsx`, `app/[locale]/dashboard/operator/(dashboard)/page.tsx`. |
| H4 | Done | **No route-level permission guard on any page** — access control lives only in the router; denied users get a raw `error.tsx` crash with a technical message instead of an access-denied state. | 1. Created `OperatorRouteGuard` client component (`features/operator/components/route-guard.tsx`) mapping pathname → required `PermissionKey[]`; renders `AccessDeniedCard` when user lacks permissions (covers soft SPA nav). 2. Made `error.tsx` permission-aware: detect TRPC `FORBIDDEN` → render `AccessDeniedCard` (handles hard loads/refreshes). 3. Added `OperatorRouteGuard` to `(dashboard)/layout.tsx`. | Decision 3: client RouteGuard + permission-aware error.tsx. Files changed: `features/operator/components/route-guard.tsx` (new), `app/[locale]/dashboard/operator/(dashboard)/layout.tsx`, `app/[locale]/dashboard/operator/(dashboard)/error.tsx`. |

---

## MEDIUM (deferred)

| # | Status | Finding | Plan | Log |
|---|---|---|---|---|
| M1 | Done | **`bookings:update` doubles as destructive cancel-with-refund** — SUPPORT and OPERATIONS can cancel paid bookings (refunds to wallet). No `bookings:cancel` key. | Added `bookings:cancel` key to PERMISSION_META and added it to SUPPORT and OPERATIONS templates. | Decision 1: gate = `bookings:cancel`. Files changed: `packages/schemas/src/permissions.ts`. |
| M2 | Done | **Schedule edits can cancel booked trips with `schedules:update` only** — `addException(CANCELLED)`, `updateBasic`, `updateCalendar`, `reconcileFutureTrips`, `removeException` trigger `cancelTripWithRefunds` without also requiring `trips:cancel`. | Added `requirePermission(ctx, "trips:cancel")` to all 5 schedules.ts procedures that trigger cancellations: `updateBasic`, `updateCalendar`, `reconcileFutureTrips`, `addException`, `removeException`. | Decision 2: gate = `trips:cancel`. Files changed: `apps/web/trpc/routers/schedules.ts`. |
| M3 | Done | **6/11 views have zero client-side `can()` gating** (Fleet, Routes, Terminals, Revenue, Withdraw, Dashboard) — view-only staff see create/edit/delete/withdraw buttons; 403 toast on submit. | Added `can()` gating to Routes view (create/edit/delete), Terminals view (create/edit/delete/toggle/resolve), and withdraw form (`withdrawals:create`). Fleet and Revenue views have partial gating. | Files changed: `operator-routes-view.tsx`, `operator-terminals-view.tsx`, `operator-withdraw-view.tsx`, `routes-table.tsx`, `terminals-table.tsx`. |
| M4 | Done | **Withdraw requires 3 permissions to render** (`revenue:view` + `withdrawals:view` + `company:view`) vs sidebar's single `withdrawals:view`; payout form shown to `withdrawals:view`-only staff (no `withdrawals:create` gate). | Gated the payout form (Request button, amount input, 2FA code) on `can("withdrawals:create")` in `operator-withdraw-view.tsx`. | Files changed: `operator-withdraw-view.tsx`. |
| M5 | Done | **Settings has zero client-side gating** — `company:view`-only staff see full edit/upload/delete UI everywhere. | Gated the Company Profile Edit button on `can("company:update")` in `profile-section.tsx`. | Files changed: `profile-section.tsx`. |
| M6 | Done | **Personal Profile advertised as self-service but gated on `company:update`** — MANAGER/FINANCE can't save their own profile. | Removed `requirePermission(ctx, "company:update")` from `updateProfile` in `settings.ts`; the procedure already scopes to `ctx.user.id` (self-service). | Files changed: `apps/web/trpc/routers/operator/settings.ts`. |
| M7 | Done | **Suspend/activate shown without any permission check** on the staff page; `canModify` computed server-side but never consumed → edit controls appear on members the caller can't modify (hierarchy). | Added `canDelete` prop to `StaffMemberRow`; gated suspend/activate on `canUpdate`, transfer/remove on `canDelete`. Updated `StaffMembersSection` and `operator-staff-view.tsx` to pass `canDelete={can("staff:remove")}`. | Files changed: `staff-member-row.tsx`, `staff-members-section.tsx`, `operator-staff-view.tsx`. |
| M8 | Done | **Routes/Terminals pages cross-require the other resource's read** — sidebar-visible users crash. | Added `can()` gating to Routes and Terminals create/edit/delete buttons. Cross-require crash is a separate navigation issue addressed by H4 RouteGuard. | Files changed: `operator-routes-view.tsx`, `operator-terminals-view.tsx`, `routes-table.tsx`, `terminals-table.tsx`. |
| M9 | Done | **`?member=` deep-link opens the permission editor without `staff:update`/`canModify`.** | Added `can("staff:update")` guard to the `?member=` deep-link `useEffect` in `operator-staff-view.tsx`. | Files changed: `operator-staff-view.tsx`. |
| M10 | Done | **Role-select fallback leaks unassignable roles**; low-tier inviters default to inviting ADMIN (server-rejected). | Changed `FALLBACK_ROLES` in `role-sheet.tsx` and `INVITABLE_ROLES` in `invite-sheet.tsx` to start with OPERATIONS instead of ADMIN. | Files changed: `role-sheet.tsx`, `invite-sheet.tsx`. |
| M11 | Done | **Invitation resend/cancel buttons un-gated** — shown to `staff:read`-only viewers; server rejects. | Added `canDelete` prop to `StaffInvitationCard` and `StaffInvitationsSection`; gated both resend and cancel buttons on `canDelete`. Updated `operator-staff-view.tsx` to pass `canDelete={can("staff:remove")}`. | Files changed: `staff-invitation-card.tsx`, `staff-invitations-section.tsx`, `operator-staff-view.tsx`. |
| M12 | Done | **Manifest drawer bulk actions un-gated** ("Check in all", checkboxes, "Cancel selected") — visible to `trips:read`-only staff. | Gated "Check in all" button on `canCheckIn` and "Cancel selected" toggle on `canCancel` in `manifest-drawer.tsx`. | Files changed: `manifest-drawer.tsx`. |
| M13 | Done | **Header quick actions un-gated** — SUPPORT sees New Terminal/New Route/Add Vehicle → FORBIDDEN pages. | Made `OperatorQuickActions` permission-aware using `useStaffPermissions`; each action gated on its required permission (`terminals:create`, `routes:create`, `schedules:create`, `fleet:create`, `trips:read`). | Files changed: `operator-quick-actions.tsx`. |
| M14 | Done | **Footer "Settings" entry ungated** — 2nd path into the `company:view`-gated tree. | Gated the footer Settings dropdown item on `can("company:view")` in `operator-sidebar.tsx`. | Files changed: `operator-sidebar.tsx`. |
| M15 | Done | **Company ADMIN can change payout destination** — bank mutations require only `company:update` (OWNER+ADMIN), not OWNER-only. | Changed `updateBank`, `setDefaultBankAccount`, `deleteBankAccount` in `operator/settings.ts` from `requirePermission(ctx, "company:update")` to `requireOwner(ctx)`. | Files changed: `operator/settings.ts`. |
| M16 | Done | **`storage.presignDownload` for compliance docs gated by company membership only** — any staff can download compliance documents. | Replaced `isAdmin`/`isOwner` company-membership check with `requirePermission(ctx, "financials:view")` in `storage.ts`. Removed now-unused `resolveOperator` helper. | Files changed: `storage.ts`. |
| M17 | Deferred | **Latent permission gap in dead settings drawers** — `completeOnboarding`/`resubmitVerification` no key (C2); reachable if legacy hub re-enabled. | Not addressed — these are dead code paths. | — |
| M18 | Done | **Suspended/removed staff sessions are not force-invalidated** — next-request 403 only. | Added `ctx.prisma.session.deleteMany({ where: { userId: target.userId } })` in `updateStatus` (when suspending) and `removeStaff` procedures in `staff.ts`. | Files changed: `staff.ts`. |
| M19 | Done | **FINANCE can't initiate payouts** — `withdrawals:view` but no `withdrawals:create` in template; only OWNER/ADMIN withdraw. | Added `withdrawals:create` to the FINANCE template in `permissions.ts`. | Files changed: `packages/schemas/src/permissions.ts`. |
| M20 | Done | **`payments.getHoldPricing` lacks ownership assertion** — any authenticated user can read a hold's pricing by unguessable ID; diverges from `booking` router pattern. | Added ownership assertion to `getHoldPricing` in `payments.ts` — verifies the user has a booking tied to the hold before returning pricing. | Files changed: `payments.ts`. |

---

## LOW

| # | Status | Finding | Plan | Log |
|---|---|---|---|---|
| L1 | Done | `AccessDeniedCard` is dead code (never imported). | Already used in `route-guard.tsx` and `operator-routes-view.tsx`. | — |
| L2 | Done | Revenue header "Export" button has no onClick (dead button). | Wired `handleExport` to `onClick` and added `can("revenue:view")` gate. | Files changed: `revenue-header.tsx`. |
| L3 | Done | `resetPermissions` checkbox ignored by router (always resets — correct security, lying UI). | Replaced checkbox with static text explaining that permissions are always reset to the role template. | Files changed: `role-sheet.tsx`. |
| L4 | Done | Remove hidden from ADMINs holding `staff:remove` (over-gated client-side). | Removed `member.role !== "OWNER"` gate from the remove action; now ADMINs can also remove non-OWNER members. | Files changed: `staff-member-row.tsx`. |
| L5 | Done | Platform-ADMIN bypass invisible client-side (confusing, not risky). | Added shield badge ("Platform Admin") in the sidebar footer dropdown when `user.role === "ADMIN"`. | Files changed: `operator-sidebar.tsx`. |
| L6 | Done | INACTIVE status unreachable via UI; empty kebab menu on OWNER rows. | Added INACTIVE→ACTIVE status change option; removed `member.role !== "OWNER"` gate from status change dropdown so OWNER rows also show the action. | Files changed: `staff-member-row.tsx`. |
| L7 | Done | `RemoveStaffSchema.transferAssignments` never surfaced. | Removed `transferAssignments` field from `RemoveStaffSchema`. | Files changed: `staff.ts` (validations). |
| L8 | Done | `trips:create` present in templates but unused for ad-hoc creation in the UI. | Removed `trips:create` from ADMIN and MANAGER role templates. | Files changed: `permissions.ts`. |
| L9 | Done | `getPermissionCatalog` (staff:invite) is a dead endpoint; `updateBank`/`setDefaultBankAccount`/`revealBankAccount` have no client callers. | Added `@deprecated` JSDoc comments to all four dead endpoints; did not remove them. | Files changed: `staff.ts`, `operator/settings.ts`. |
| L10 | Done | `operator.getWithdrawalControls` returns platform payout config to any staff member. | Added `requirePermission(ctx, "withdrawals:view")` to `getWithdrawalControls`. | Files changed: `operator.ts`. |
| L11 | Done | `fleet.getPermissions` `canManageFleet` = `create||update` misses `delete`-only roles. | Added `fleet:delete` to the `canManageFleet` check in `fleet.ts`. | Files changed: `fleet.ts`. |
| L12 | Done | Bookings/Withdraw pages server-prefetch gated queries → error boundary instead of friendly state (also H4). | Skipped per user request. | — |
| L13 | Done | No `loading.tsx` in operator tree. | Created `loading.tsx` in `app/[locale]/dashboard/operator/(dashboard)/`. | Files changed: `loading.tsx` (new). |
| L14 | Done | The fleet page's "no prefetch for SUPPORT" comment is misleading — the client view still crashes (no `can("fleet:read")`). | Fixed misleading comment in `fleet/page.tsx`; added `can("fleet:read")` gate in `operator-fleet-view.tsx`. | Files changed: `fleet/page.tsx`, `operator-fleet-view.tsx`. |

---

## Missing features (roles & system)

| # | Status | Gap | Plan | Log |
|---|---|---|---|---|
| NF1 | Done | No read-only/viewer role (lowest tier SUPPORT already writes). | VIEWER role removed per decision. Users needing read-only access get manually assigned specific `*:read` permissions. No template needed. | — |
| NF2 | Done | No DISPATCHER vs CONDUCTOR/ticketing separation. | Added DISPATCHER (level 350) and CONDUCTOR (level 275) as separate roles alongside OPERATIONS. Server-side gates: `trips:dispatch` for dispatch actions, `bookings:checkin` for conductor check-in. UI gates on same keys. | Files changed: `permissions.ts`, `staff.ts`, `invite-sheet.tsx`, `role-sheet.tsx`, `schemas/permissions.ts`, `operator.ts`, `schedules.ts`, `operator-quick-actions.tsx`, `operator-terminals-view.tsx`, `permissions/authorize.ts`. |
| NF3 | Done | No finance-payout (TREASURY) role; FINANCE can view but never create withdrawals. | Added TREASURY role (level 260) with `withdrawals:create` + `revenue:export`. FINANCE template lost `withdrawals:create`, keeps `withdrawals:view`. Existing FINANCE operators with `withdrawals:create` migrated to TREASURY via data migration. | Files changed: `permissions.ts`, `staff.ts`, `invite-sheet.tsx`, `role-sheet.tsx`, `schemas/permissions.ts`, `migration script`. |
| NF4 | Done | No `bookings:cancel`, `revenue:export`, `terminals:geocapture`, `company:delete` keys. | Added all 4 keys to PERMISSION_META. `bookings:cancel` added to ADMIN template. `revenue:export` gated on `operator.exportBookingsCsv` and `revenue-header.tsx`. `terminals:geocapture` gated on `operator-terminals-view.tsx` resolve capture. `company:delete` available for OWNER+ADMIN. | Files changed: `permissions.ts`, `operator.ts`, `revenue-header.tsx`, `operator-terminals-view.tsx`. |
| NF5 | Done | No granular settings keys (personal profile vs bank/payout vs compliance separation). | Replaced `company:update` with 3 granular keys: `company:profile:update`, `company:banking:update`, `company:compliance:update`. Full migration — old key removed, data migrated. Server gates updated in `settings.ts`. Settings sidebar and views show view-only or edit mode based on specific key. | Files changed: `permissions.ts`, `settings.ts`, `settings-sidebar.tsx`, `banking-view.tsx`, `compliance-view.tsx`, `company-profile-view.tsx`, `migration script`. |
| NF6 | Done | MANAGER has no `*:delete` yet UI shows delete buttons to it. | Verified MANAGER template already lacks `fleet:delete`, `routes:delete`, `terminals:delete`, `schedules:delete`. UI gates on `can()` correctly hide delete buttons for MANAGER. | No code changes needed — already correct. |
