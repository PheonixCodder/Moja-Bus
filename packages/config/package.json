{
  "name": "@moja/config",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shared runtime and environment helpers for Moja Buss workspaces.",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "tsconfig.json"
  ],
  "scripts": {
    "build": "echo \"No build configured for @moja/config\"",
    "lint": "biome check package.json tsconfig.json src",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests configured for @moja/config\""
  },
  "devDependencies": {
    "@moja/typescript": "workspace:*"
  }
}
