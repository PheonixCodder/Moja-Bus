{
  "name": "@moja/types",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "tsconfig.json"
  ],
  "scripts": {
    "build": "echo \"No build configured for @moja/types\"",
    "lint": "biome check package.json tsconfig.json src",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests configured for @moja/types\""
  },
  "devDependencies": {
    "@moja/typescript": "workspace:*"
  }
}
