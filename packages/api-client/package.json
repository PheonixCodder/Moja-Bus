{
  "name": "@moja/api-client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shared HTTP client helpers for Moja Buss workspaces.",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "tsconfig.json"
  ],
  "scripts": {
    "build": "echo \"No build configured for @moja/api-client\"",
    "lint": "biome check package.json tsconfig.json src",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests configured for @moja/api-client\""
  },
  "dependencies": {
    "@moja/shared": "workspace:*"
  },
  "devDependencies": {
    "@moja/typescript": "workspace:*"
  }
}
