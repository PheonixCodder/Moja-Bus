> **Canonical Session Memory** — Updated by `/remember save` at end of every session. Restored by `/remember restore` at start of every session. This is the ONLY memory file in the repo. See [CONTEXT_SYSTEM.md](./CONTEXT_SYSTEM.md) for protocol.

# Memory — Driver System Release Remediation + CDD Ecosystem Restructure

Last updated: 2026-08-25 (ALL 39 phases complete — remediation track closed; staging & release next)

## State

- **🏁 ALL 39 PHASES (00–38) COMPLETE ✅ (2026-08-25) — v2 remediation track CLOSED.** All 93 audit findings fixed in code. Phases 32 (timing-safe webhook compare "Phase 32 (F-PS-13)" at paystack-client.ts:143 + ticket-sheet QR caption shows bookingReference not bearer token), 33's remaining legs (PS-11 guest claim rebuilt on `phonesMatch` predicate; PS-10 COMPLETED status reads in booking-read-service.ts; PS-16 rebooking→outbox landed earlier), 36 (`cron-hygiene.test.ts` route↔schedule parity guard, TELEMETRY_TOKEN_SECRET in both env examples, junk files deleted), 37 (release-escrow zero `$queryRawUnsafe`, escrow alerts day-dedupe F-IN-13), and 38 (web tickets view + traveler tracking screen fully i18n'd) landed via PARALLEL sessions — user ratified completion and each marker was code-verified into the trackers on 2026-08-25 before ticking. **Remaining work is staging & release ONLY**: Gate A/B/C probes per `12-release-checklist.md`; CSP report-only→enforcing flip after zero-violation console pass; EAS build proof for rnmapbox pair; device legs (camera scan, Novu delivery, Paystack checkout, push tap-routing QA ×3 surfaces); Phase 35's added legs (header dump, geo-capture, browser scanner QA, OTP regression).
- **Phases 34+35 CODE COMPLETE (2026-08-25)** — notification small-fixes + web security headers/origins. F-NF-11/12/13/14/15 + F-IN-08/09/10/16 closed. Key rulings executed: retry resets attempts:0 (D34-1a — human-triggered only so poison can't loop; disjoint from worker claim sets); pauseReason rename + harness rows, D34-2b-i FOLLOW-UP FILED IN-CODE at discounts/services/notify.ts: "discounts notify family → outbox" is its own future session, never cherry-pick helpers; NF-13 pre-closed by Phase 24 day-buckets, same-day SUSPEND→RESTORE→SUSPEND silence ratified+documented at marketplace-admin.ts; conflict alert email optional BOTH sides + busyUntil now travels ISO (workflow formats fr-FR w/ UTC=CI-local comment); **F-NF-15 = client-side identifier→route maps on all three surfaces** (`lib/notification-routes.ts` driver+traveler, `components/notification-routes.ts` web) — SDK verification DONE: Novu in-app messages carry workflow.identifier + trigger payload data + redirect, so NO workflow edits needed and parameterized routes degrade to static screens gracefully; map wins over redirect fallback; cross-surface (/dashboard) redirects never followed on mobile; web pins operator-account-suspended → null (no-nav into dashboard for suspended operators); web Inbox switched to children form (<Inbox appearance> + <InboxContent onNotificationClick> — appearance STAYS on Inbox even in children mode). Driver notifications screen got own i18n namespace (was borrowing "offers"). Phase 35: CSRF extracted to lib/mutation-origin.ts (malformed→FORBIDDEN not INTERNAL; prod pins https://host; dev keeps host-equality; no-Origin bypass documented for natives) 7-case suite; images.remotePatterns first-party ONLY (cdn.mojaride.com + S3_PUBLIC_URL_BASE hostname parsed config-time), editor surfaces (blog×3+banners×2) use `unoptimized` prop instead of allowlist widening — Google avatars ride Radix plain <img>, unaffected by remotePatterns; Caddy Permissions-Policy geolocation=(self),camera=(self),microphone=() SITE-block-only (un-broke browser QR scanner + geo-capture); CSP REPORT-ONLY baseline w/ compose-fed POSTHOG_PUBLIC_HOST/S3_PUBLIC_HOST defaults — enforcement flips after zero-violation staging QA, nonce/hash CSP explicitly deferred; trustedOrigins via lib/trusted-origins.ts (prod: baseUrl+explicit ALLOWED_ORIGINS+app schemes only; exp://+localhost:8081+six localhost defaults dev-gated; EXPO_DEV_ORIGIN env = explicit recovery path honored anywhere). ⚠️ GATE LESSON: first turbo run showed green from CACHE while web tsc actually had errors (incl. duplicate function my edit left + pre-existing outbox-cadence-guard TS4111 + next.config RemotePattern type) — always --force or check Cached:0. Final gates: turbo typecheck+test **19/19 UNCACHED** · web **543/543** · driver-app **20/20** · schemas 86 · biome clean on new files. Migrations: ZERO. Staging legs: header dump, geo-capture QA, browser scanner QA (new), OTP login regression both apps, CSP console review. Parallel session note: Phase 33's F-PS-16 (rebooking→outbox) landed in-tree mid-session (workflow+enqueue+service+contract row); PS-10/PS-11 still open.

## Locked decisions (Phase 17, user-ratified)

- D1=A verifyDriver: keep platform-wide semantics + active-roster scoping (no operator/admin split)
- D2=A remove DRIVER Operator rows entirely; D2b=add `UserRole.DRIVER` enum for placeholder accounts
- D3=A guided credential handoff (confirm dialog + phone-OTP instructions; no SMS infra dependency)
- D5=wire `operator-bank-verified/-rejected` via outbox; ⚠️ `admin-bank-account-pending` UNWIRED by design — Paystack self-verifies at save so no pending event exists; ruling pending (delete or wire later)
- D6=fix ticket-share domain AND ship Share UI (done: web tickets sheet + traveler booking detail)
- D7=refund notice enqueued inside CancellationService tx, all paths (`notifyRefunded=false` on trip-cancel path)

Earlier commercial-lifecycle decisions (D1–D8 of the hardening plan) live in git history of this file — superseded context, see docs/plans/commercial-lifecycle-hardening/.

## Prior state

- **Phases 28+29+30+31 CODE COMPLETE (2026-08-25)** — F-TM-07/08/09/13/14/15/16/17/18/19 + F-DV-11/14 closed, 4 unnumbered observations cleaned. Key rulings: jump gate reads DriverProfile.last* (NOT a redis hash — delete-arm won); LOW_ACCURACY takes precedence over overspeed in derivePingAnomaly (bad fix poisons speed too); Redis = optional pub/sub backend only, loud boot banner + permanent logged downgrade, memory is official v1 posture; pay rate = PlatformSettings COLUMN not env var (env drift would make driver money differ per environment); urgent-dispatch acks live on the assignment row and ACK-RESET is ruled: in-window delays stay silent by design (trips surface owns delay comms); earnings totals are GLOBAL across carriers by design (do not "fix" to per-company — silently changes multi-affiliated drivers' numbers); updateDriver.status STRIPPED (D8-a: sole caller never sent it). Review caught one audit blind spot via mechanical grep: search-map-view.tsx had attributionControl:false PLUS deliberate CSS flag suppression on raw OSM tiles — fixed with the other three surfaces. Process lesson recorded: compliance sweeps enumerate surfaces mechanically, never inherit the auditor's list. Fixed en route: interrupted-session web tsc breakage (@prisma/client import, WS lastPing type), dead nightly penaltyRows query, driver-app biome.jsonc broken extends + missing root:false (lint had never run there). Gates 19/19 · web 496 · driver 14. Neon FULLY CONVERGED 2026-08-25 — user ran `prisma migrate deploy`; exactly ×2 phase-31 migrations were pending (the "4 pending" tracker estimate was stale — the 0824 pair had already been applied in an unrecorded earlier run). Remaining phases: **32–38**. Staging legs: EAS build proof (rnmapbox pair + attribution screenshots), reconcile before/after delta, ack-survives-reinstall probe.
- **Phases 25+26+27 CODE COMPLETE (2026-08-25)** — Wave-4 opener trio; F-OP-09…15 closed. Rulings made this session: (a) `drivers:verify.*` keys SEEDED into ADMIN role template (empty-permissions fallback would otherwise strip hub access from every non-SUPER_ADMIN at deploy); (b) verification-outcome notice rides the durable OUTBOX in-tx (`DRIVER_VERIFICATION_OUTCOME`, day-bucketed per action — SUSPEND→RESTORE→SUSPEND same-day fires both), NOT direct novu.trigger (first session's transport, found defective on review); (c) public-profile redaction is CONDITIONAL (`isAvailableForHire && !isSuspended`) via shared `PublicDriverProfileView` type — first session's unconditional version killed contact data platform-wide + regressed F-OP-06 + silently dropped trustBadges behind an `as any`; (d) add-driver licence uploads go under the OPERATOR's storage namespace (purposes are caller-scoped, dossier presigns are reader-agnostic server-side); (e) APPROVE with zero compliance docs refused on BOTH verify paths (REJECT/SUSPEND stay open for legacy cleanup); (f) canAssign re-backed to enforced key trips:update. Latent bugs fixed en route: duplicate workflow registration in index.ts (from interrupted session); createDriver/updateDriver `.url()` doc constraint rejected Phase-15 storage keys → `driverDocReferenceSchema`; `licenseExpiryDate` missing from listAssignableDrivers select made the Phase-14 licence gate inert there. New pure core `findTripConflict` shared by single+batch conflict paths (11-case suite wired). Gates: schemas tsc ✓ · web tsc ✓ · turbo test 9/9 · web **461/461**. Ride-along (2026-08-25): `admin.listDriversForVerification` now mirrors getDriver's key→presigned-GET swap — Phase-15's "documents visible in both hubs" leg was structurally broken on the admin side (raw keys → placeholders). Remaining phases: 28–38. Staging legs for 25–27 per phase files.

- **v2 FULL-SYSTEM AUDIT DELIVERED (2026-08-22)**: `context/drivers/full-system-e2e-audit/` — six parallel deep audits + lead first-hand verification of the entire driver app. **93 unique findings: 1×P0 · 10×P1 · 31×P2 · 51×P3** (98 raw records, 5 cross-domain dups). Supersedes the 40-finding audit for release-gating; all 5 prior P0s verified FIXED in code.
- v2 headline blockers: **F-DV-01 P0 migration tree unreproducible from git** (baseline enum drift EN_ROUTE/ON_BREAK/etc vs ON_DUTY/ON_TRIP + five untracked migration dirs — fresh deploys fail); F-PS-03 scanner can't read URL-wrapped QR tokens; F-IN-01 check-in has no assignment/tenancy binding; F-PS-01 verifyPayment no ownership assert; F-PS-02 PAYSTACK refund channel never settles (maps COMPLETED, invisible in OWED queue); F-DV-04 cancelled/suspended runs strand drivers ON_TRIP forever; F-NF-01/02 two passenger notifications violate own payload schemas; F-NF-03 eight audiences still email-keyed; F-TM-01 WS gateway hosted nowhere in prod; F-IN-05 CI ships with zero tests.
- Remediation order: **39-phase execution plan delivered** — `full-system-e2e-audit/13-phased-execution-plan.md` + one file per phase (`phase-00…38`). All 93 IDs mapped to exactly one phase; waves: foundation (00–01) → blockers (02–09) → hardening (10–22) → polish (23–38). Root `context/progress-tracker.md` carries the checkbox list.
- **Phase 00 EXECUTING (2026-08-23, D1–D4 ratified)**: branch `phase-00-db-reproducibility` — six untracked migration dirs committed, enum-repair split ×2 (`20260823000000_…_values` / `…_data`, repo's enum-in-tx precedent), `db-drift.yml` CI gate (postgis:16-3.4 shadow + `migrate diff --exit-code`), `deploy/maintenance/rehearse-clean-deploy.sh`, `packages/db/MIGRATIONS.md` policy (db-push ban + resolve-playbook), `scripts/inspect-migration-state.ts`. KEY FINDING: prod runs legacy driver enums TODAY (CI images build from git; six dirs never reached the migrate image). Pending: introspection run (D10) on live envs, merge → green drift gate, rehearsal.
- **Phase 07 CODE COMPLETE (2026-08-23)**: F-NF-01/F-NF-02 fixed per user-ratified D1–D7. Cancelled payload: bookingReference + unconditional refundAmountXOF + refundStatus/refundChannel enums; template copy branches truthfully (wallet credit / CASH manual settlement / failed-refund). BOTH delay paths now enqueue via shared outbox helper `enqueuePassengerTripDelayed` (TRIP_DELAYED type, hourly-bucketed transactionIds) — direct novu.trigger + .catch(()=>{}) removed from trips.ts + drivers.ts. Cancelled fan-out moved INSIDE cancel tx. Contract harness `features/notifications/__tests__/payload-contracts.test.ts` (9 cases incl. audit-defect regression tripwires); payloadSchemas now exported consts. D7: SENT=accepted-by-Novu documented at process.ts marking site. Gates 18/18, web 422/422. Staging probes pending.
- **Phases 13+12+11 CODE COMPLETE (2026-08-23)**: Phase 11 initially deferred then REVERSED after decomposition review — its scope was purely driver-token authz (operator-subscriber design was never in scope, stays revival-gated). Landed: signed `c` company claim at both mints (`mintTelemetryDispatchTokenWithCompany`, verify tolerates legacy c-less tokens but rejects malformed types), enforced-mode subscribe ACL restricted to `trip:${claims.t}` via pure `isRoomAllowedForClaims`, `operator:{c}:fleet` publish now flows under enforcement, HTTP-path gap documented in-code. New 8-case suite `lib/__tests__/telemetry-token.test.ts`. Phase 13 — passport Edit/Remove dialogs (permission-gated) with server-side mid-run CONFLICT guard on deleteDriverAffiliation, atomic soft-delete+notice via new fr-first `driver-roster-removed` workflow + `enqueueDriverRosterRemoved` helper, roster accumulate pagination, F-IN-02 ride-along (updateDriver isActive:true). Phase 12 — live HUD real GPS (simulator deleted, "—" till first fix), honest Trip ETA from Directions durationSeconds ("Route Progress" card), "Fixed 5 s" label, zombie-stop preserved. Revival checklist now: host image + Caddy passthrough + operator-subscriber credentials + consumer client. Gates 19/19 · driver-app 10/10 · web 440/440.
- **Phases 09+10 CODE COMPLETE (2026-08-23)**: D1 challenged + ratified — **Option B HTTP-only v1** (B = zero-reversal-cost prefix of future A; gateway kept dormant w/ revival checklist tied to live-tracking consumer). WS localhost default gone (skip-unless-set), reconnect budgeted 5×exp/segment. Phase 10: chunked ≤100 flush (remainder preserved, drops logged); **D5 CORRECTED from audit** — harsh braking = deceleration severity (≥25 km/h AND ≥2.8 m/s² AND Δt≤8 s), NOT naive window-widen (that flagged everyday bus-stop braking at ~1.2 m/s² → fleet-wide false positives); 401 re-mint via injected handler (`setTelemetryReauthHandler`, registered in trips.tsx via queryClient.fetchQuery) + needsReauth flag. KEY COUPLING: old flush trigger was WS-open-only — replaced with start/ping-success/60s sweep or Option B breaks draining. New `lib/telemetry-core.ts` (pure, Hermes+node:test safe); driver-app `"test"` script wired → first mobile suite in turbo. Gates 19/19 · driver-app 10/10 · web 432/432.
- **Phase 08 CODE COMPLETE (2026-08-23)**: F-NF-03 fixed per user-ratified D1–D7 — all nine broken trigger sites re-keyed subscriberId=user.id (invitation staff-acceptance incl. invitedBy.id select fix for the fallback-to-wrong-person edge; profile-updated; review-submitted; user-role-updated; account-suspended/restored loops; withdrawal-resolved; FORCE_FAIL payout fan-out; escrow treasury alert closing the email/id split vs operator.ts). Adjacent fixes on touched lines: Date.now() txIds → stable/day-bucketed (escrow = F-IN-13 daily-resend CLOSED EARLY), passenger .catch(()=>{}) → loud logging. Contract harness extended to 19 cases / 11 workflows (all 9 audience schemas exported as consts). Gates 18/18 · web 432/432 · biome clean. Follow-up filed: admin fan-out audience policy (role=ADMIN vs active AdminStaff holders) — deferred ruling. Staging leg pending (needs Novu staging env).
- **PROBES 6/6 PASSED (2026-08-23)**: `apps/web/scripts/probe-phases-02-06.ts` executed vs converged scratch DB (real PG16): P02 URL-wrapped QR boards · P03a cross-tenancy FORBIDDEN (zero mutation) · P03b unpaid PRECONDITION_FAILED · P04 foreign verifyPayment FORBIDDEN pre-Paystack, hold untouched · P05 PAYSTACK channel rejected at guard · P06 converge clears ON_TRIP→AVAILABLE/OFFLINE + zero ghosts. Harness is re-runnable (idempotent pb_ seed; P02 resets boarding). Remaining Gate-A legs = physical device/staging only (camera scan, Novu 3-channel delivery, owner-path Paystack checkout, UI passes).
- **Phases 23+24 CODE COMPLETE (2026-08-23)**: Phase 23 — fleet map rebuilt as REAL poll-fed Leaflet map (new `fleet-live-map.tsx`, CARTO dark tiles, ssr-false dynamic import); radar deleted; three-state freshness (fresh ≤5 min / dimmed ≤24 h / hidden >24 h) on markers AND roster filter; popups name·plate·speed·ping; selected-vehicle HUD kept. Phase 24 — public-profile sheet Send-Offer disabled when isOnMyRoster (procedure response exposes it via companyId in affiliations select); sent-offers accumulate pagination (page batches + reset on tab change); marketplace featured/suspended keys day-bucketed (F-NF-13 remainder closed). Gates 19/19 · web 447 · biome clean.
- **Phase 22 CODE COMPLETE — NOTIFICATION SUBSYSTEM FULLY HARDENED (2026-08-23)**: F-NF-07 wired (notifyExhaustedCampaignBudgets console.log orphan → durable outbox enqueues via new CAMPAIGN_BUDGET_EXHAUSTED type + campaigns.ts helper, day-bucketed per operator, recipients=companyOperatorRecipients). F-NF-08 ruling EXECUTED = DELETE admin-bank-account-pending (Paystack self-verifies at save ⇒ honest pending event impossible; file deleted + inline ruling in index.ts; user should archive Novu-dashboard twin). F-NF-10 resolved-by-evidence + guarded: prod crontab ALREADY every-minute (≈1 min latency beats */5 ask); new outbox-cadence-guard.test.ts asserts the line stays in CI (vercel.json documented non-prod, hourly forced by Vercel free tier). DEAD-math note: prod worst-case ≈3.5 h at maxAttempts 8. Gates 19/19 · web **447/447** · biome clean.
- **Phases 20+21 CODE COMPLETE (2026-08-23)**: F-NF-04 — recipient-scoped txIds moved INSIDE the 5 multi-recipient offer helpers + conflict alert (`txIdWithRecipient`/`recipientTag`: truncated SHA tag, NOT raw subscriberId — length+privacy; collision-freedom unit-tested in tx-id.test.ts). F-DV-13 — both lazy sweeps routed through existing expireOfferIfDue in-tx. CTA util `dashboardUrl()` fixed payout-failed + verification-approved/rejected (approved was pointing OPERATORS at admin.mojaride.com surface). Phase 21: expo.data overrides on all 11 traveler push workflows matching handler map verbatim (3 unmapped stay override-less by design); NEW driver push steps ×7 (offers received/counter/expiring/expired + assigned/unassigned + dispatch-urgent) fr-first; driver handler gained dispatch-urgent case → /(tabs)/trips. F-NF-06: registerPushToken update→**credentials.append** (Novu native append; dual-app users keep both tokens; platform input informational-only documented). Excluded: marketplace/license/roster pushes, campaign routing, Expo feedback pruning. Gates 19/19 · web 445 · driver 10 · schemas 86.
- **Phases 18+19 CODE COMPLETE (2026-08-23)**: F-PS-05 notice half verified ALREADY CLOSED by Phase 04 pull-forward (both initiators stamp metadata.userId; gate reads it) → deleted dead wallet router entirely (zero callers anywhere; callback targeted nonexistent page). F-PS-04: traveler cancel dialog consumes passenger.getRefundQuote per-seat (server truth), non-cancellable note + toast errors. F-PS-06: deep-link fixed to /booking/[reference]. F-PS-07: review-request CTA → getAppOrigin()/dashboard/bookings?tab=past (+in-app redirect), payloadSchema exported + contract-harness row added. F-PS-08: ReviewSheet explicit-only (overall starts 0/disabled until chosen; sub-ratings null and keys OMITTED — sending explicit null fails Zod). F-PS-09: submitReview rejects unless booking.completedAt != null (same predicate as getPendingReviews; legacy-null trips stay unreviewable by design). Gates 19/19 · web 441 · biome clean.
- **Phases 14+15+16+17 CODE COMPLETE (2026-08-23)** — corrections caught by user challenge, both material: (a) licence gates compare vs trip estimatedArrival NOT now (Monday-assign/Sunday-run trap); (b) driver delay persistence must mirror operator formula EXACTLY or dispatch formalization double-counts delayMinutes. Landed 14: trip-aware gates on assign/list/urgent/startTrip + VERIFIED-only operate policy + nightly expire-driver-licenses cron (month-bucketed warnings) + expiry badges. 15: wizard presign-uploads (file:// era over), nationalIdNumber column (migration rehearsed), employmentType→servicePreference.preferredType + affiliation, affiliated flag + honest no-carrier state, presigned dossier GETs + legacy-URI placeholders, documents.tsx div-crash fixed. 16: updateMyStatus one-authority matrix, INVITABLE_STAFF_ROLES excludes DRIVER server-side (+shared UI constant; OPERATIONS→DRIVER assignment stays legal), role input:false, E.164 phone + PHONE_REVERIFICATION_REQUIRED parsed by wizard, OTP log gated (caller audit: zero UI consumers of updateMyStatus). 17: shift ledger deterministic (hiredAt fallback/double-open REJECT/company-bound close) + partial-unique-index migration WITH duplicate pre-close repair (would brick otherwise); reportTripDelay persists cumulative+status(pre-departure only)+time-shift+conflict alerts. Gates 19/19 · web 440 · driver-app 10 · schemas 86. NOTE: migrations 20260824000000/01 landed AFTER Neon run → Neon needs one more `migrate deploy`; custom server still introspection-pending.
- **NEON MIGRATED ✅ (2026-08-23)**: backup branch created by user → `migrate deploy` applied the six pending migrations + enum repairs + convergence. First attempt FAILED (`BannerActionType already exists` — Neon is db-push-MIXED: some datamodel objects pre-existed) → recovered via `migrate resolve --rolled-back` + hardened `20260823235959_phase00_schema_convergence` to fully guarded/idempotent SQL (equality-checked enum rebuilds, IF-NOT-EXISTS creates; fresh-path re-proven drift-0 on scratch). Post-state: enums exact, ledger complete, promo_banner present, service_preference/offer tables live. **Probes 6/6 re-run against REAL Neon**; pb_ seed data removed, residue sweep ALL CLEAN. Vercel testing server driver flows should now work — user smoke test pending. Rollback path: `pre-phase00-backup` Neon branch. Custom server (deploy/) still pending introspection + its own run.
- **DB STATE DISCOVERED (2026-08-23)**: ~~dev Neon PRE-migration~~ RESOLVED above. Custom server (deploy/ compose) not yet introspected — run inspect script there before its cutover; watch for quoted-CamelCase tables WITH DATA alongside lowercase ones (the guarded-merge scenario).
- **Phase 00 rehearsal EXECUTED + PASSED (2026-08-23)**: clean replay initially failed at phase17_driver_operator_cleanup — root cause was the migration hand-writing `"Operator"/"User"/"DriverProfile"` while schema @@maps them to lowercase (`operator`/`user`/`driver_profile`); fixed IN PLACE under documented never-applied exception (ruling in file header). Drift diff then enumerated more holes → new final migration `20260823235959_phase00_schema_convergence`: creates promo_banner+BannerActionType (model existed with NO migration!), rebuilds 6 enums to exact datamodel sets (ADD VALUE can't remove labels), aligns driver updatedAt defaults + referral_program.id, maps license 'A'→'B' before casts. **RESULT: full replay completes AND drift = EMPTY (exit 0)** on real PG16 (Neon scratch). db-drift.yml rewritten for current CLI (`--from-url`/`--shadow-database-url` removed — old flags would have errored in CI). Neon runbook simplified: all six NOT_RECORDED ⇒ no resolves needed anywhere, just backup branch → `migrate deploy`. Scratch DB `moja_probe_tmp` now holds a fully-converged EMPTY database — ready target for Phase 02–06 probes; drop after staging.
- **Phase 03 CODE COMPLETE (2026-08-23, verified+closed this session)**: check-in authorization binding (F-IN-01 ≡ F-DV-03) was already substantively coded — this session verified every acceptance criterion and closed the phase. `DriverCheckInService.assertBoardable` = 4-guard pipeline (tenancy via active TripDriverAssignment→FORBIDDEN · declared tripId mismatch→BAD_REQUEST · CONFIRMED-only→PRECONDITION_FAILED · window SCHEDULED/BOARDING/DELAYED/DEPARTED), wired into scan/manual/batch with session-derived ctx.driver.id; manifest no longer leaks ticketTokens; batch = per-item REJECTED outcomes, unexpected errors propagate. Ratified deviations: tripId stays OPTIONAL in schemas (server-side tenancy derivation is stronger); CONDUCTOR boards (manifest-consistent, spec said PRIMARY/RELIEF only); NO post-arrival grace (stricter than spec's optional grace). 30-case suite green; gates 18/18 (web 413/413). Staging probes pending.
- **Phase 01 CODE COMPLETE (2026-08-23)**: deploy.yml quality-gate = typecheck (traveler-app now INCLUDED — D7-A probe clean exit 0) → `turbo test` → lint; DATABASE_URL dropped from gate job; build/push still strictly `needs:`-gated. Orphan suites resolved: outbox+authorize wired green, web staff-hierarchy.test.ts deleted (D5-A), schemas roles-and-permissions.test.ts rewritten to SHIPPED RBAC model + wired (now the sole hierarchy spec; encodes DISPATCHER 350 > OPERATIONS 300 reversal, OPERATIONS→[DRIVER], DRIVER narrow template). 56/56 suites wired, 0 orphans. Gates: turbo typecheck 10/10 · turbo test 8/8 (web 413/413 · schemas 78/78) · local red/green proven (exit 1 broken / 0 restored). ⚠️ DEVIATION pending ratification: lint step is report-only (`continue-on-error`) — pre-existing biome debt across ALL 10 packages (~120 diagnostics) needs a dedicated sweep before it blocks. User actions left: CI end-to-end red/green on next deliberate master push; branch protection required checks `quality-gate` + `drift-check` (D9); ratify lint deviation.
- Tracker drift confirmed by audit: 18.4 mitigated-open / 18.5 partial / **18.6 DONE** / 19.2–19.4 substantively landed — reconcile in Gate 0.
- Prior state: Phases 16–19 code complete (typecheck green); staging gates A/B/C probes still never run.
- Open ruling: `admin-bank-account-pending` workflow fate (F-NF-08: still registered, zero triggers, decision undocumented in code)
- Note: prior-audit catalog counts were authoritative for that audit: 5×P0 · 7×P1 · 15×P2 · 13×P3 = 40

## Plan location

`context/drivers/remediation-plan.md` (task criteria) · `context/drivers/progress-tracker.md` (checkboxes)

## Next session starts with

1. **Staging & release execution** — all remediation phases (00–38) are closed; work the Gate A/B/C probe list in `full-system-e2e-audit/12-release-checklist.md` against a converged environment
2. CSP flip decision: report-only → enforcing once a staging console pass shows zero violations (nonce/hash hardening remains its own future session)
3. EAS build proof for @rnmapbox/maps JS ^10.3.5 / native pin pair (attribution screenshots)
4. Device legs: camera QR scan end-to-end, Novu email+inApp+push delivery on real workflows, Paystack owner-path checkout, push tap-routing QA on driver+traveler+web (Phase 34 route maps)

## Do not

- Reintroduce Operator rows or ERP permissions for roster drivers
- Fire notifications for events that don't exist (bank "pending" is currently unreachable)
- Double-notify on operator trip-cancel (it already sends passenger-trip-cancelled with refund amount)
- Trust client-supplied driverProfileId on telemetry ingest (16.8 tokens exist — keep enforcing)
- Cherry-pick individual discounts notify helpers onto the outbox — that family migrates as ONE future session (D34-2b-i, comment at notify.ts)
- Follow stored redirect URLs across surfaces in notification tap-routing (mobile never follows /dashboard/*, web map pins account-suspended to no-nav)
- Widen images.remotePatterns again — editor-supplied URLs use the `unoptimized` prop instead
